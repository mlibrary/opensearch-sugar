include:
  - compose_opensearch.yml
services:
  app:
    build:
      context: .
      target: ruby
    platform: linux/aarch64
    volumes:
      - ../..:/app
      - gem_cache:/gems
    # environment variables are set via env.testing
    env_file:
      - env.testing
      - .env
    command: "tail -f /dev/null"
    extra_hosts:
      - host.docker.internal:host-gateway
volumes:
  gem_cache: